<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.cmwa.ec.weixin.dao.BankLogoDao">
	
	<select id="queryBankLogoList" resultType="com.cmwa.ec.weixin.dto.BankLogoDto">
		SELECT
			BANK_NO bankNo,
			BANK_NAME bankName,
			BANK_LOGO bankLogo,
			TO_CHAR(CREATED_DATE, 'YYYY-MM-DD HH24:MI:SS') createdDate,
			TO_CHAR(UPDATED_DATE, 'YYYY-MM-DD HH24:MI:SS') updatedDate
		FROM CMWA_BANK_LOGO
	</select>
	
</mapper>